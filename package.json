{
  "name": "boco-cached-firebase",
  "version": "0.1.7",
  "description": "Cached firebase for fully offline access.",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/.bin/coffee ./test.coffee",
    "build": "rm -rf build && mkdir build && ./node_modules/.bin/coffee -cmb -o build source"
  },
  "author": "Christian Bradley <christian.bradley@bocodigital.com> (https://github.com/christianbradley)",
  "license": "MIT",
  "dependencies": {
    "bluebird": "^3.4.6",
    "json-ptr": "https://github.com/frankrue/json-ptr.git#v1.0.2"
  },
  "devDependencies": {
    "coffee-script": "^1.10.0",
    "node-localstorage": "^1.3.0"
  }
}
